import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PM-AppListaHeroes-Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
